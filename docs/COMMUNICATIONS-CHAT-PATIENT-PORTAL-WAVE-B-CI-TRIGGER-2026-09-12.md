# Wave B CI synchronization marker

This marker exists only to ensure the latest Wave B candidate head is evaluated by the existing pull-request Unified Test Execution Engine after the verification contract was added. It introduces no runtime behavior, schema, permission, messaging or test-engine changes.
