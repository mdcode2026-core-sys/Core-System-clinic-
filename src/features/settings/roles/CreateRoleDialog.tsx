"use client";

import { useState } from "react";
import { useI18n } from "@/core/i18n/I18nProvider";
import { createRole } from "@/domain/roles/roles.actions";
import { usePermissionsCatalog } from "@/domain/roles/roles.queries";
import type { PermissionRow, Workspace } from "@/domain/roles/roles.types";
import { Button } from "@/shared/components/ui/button";
import { Input } from "@/shared/components/ui/input";
import { Label } from "@/shared/components/ui/label";
import { Checkbox } from "@/shared/components/ui/checkbox";
import { Card, CardContent, CardHeader, CardTitle } from "@/shared/components/ui/card";
import { Badge } from "@/shared/components/ui/badge";
import { Separator } from "@/shared/components/ui/separator";
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/shared/components/ui/dialog";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/shared/components/ui/select";
import { Save, Loader2, AlertTriangle } from "lucide-react";

interface CreateRoleDialogProps { open: boolean; onClose: () => void; onSuccess: () => void; }
const workspaceLabels = (ar:boolean) => ({ administration: ar ? "الإدارة" : "Administrative", operation: ar ? "التشغيل" : "Operation", clinical: ar ? "السريري" : "Clinical" });

export function CreateRoleDialog({ open, onClose, onSuccess }: CreateRoleDialogProps) {
  const { data: catalog, isLoading: catalogLoading } = usePermissionsCatalog(); const { messages, terminology, locale, roleError } = useI18n();
  const [roleKey,setRoleKey]=useState(""); const [roleName,setRoleName]=useState(""); const [roleNameAr,setRoleNameAr]=useState(""); const [description,setDescription]=useState(""); const [workspace,setWorkspace]=useState<Workspace | "">(""); const [selectedIds,setSelectedIds]=useState<Set<string>>(new Set()); const [isSaving,setIsSaving]=useState(false); const [saveError,setSaveError]=useState<string|null>(null);
  const togglePermission=(id:string)=>setSelectedIds(prev=>{const next=new Set(prev);next.has(id)?next.delete(id):next.add(id);return next;});
  const handleSave=async()=>{setIsSaving(true);setSaveError(null);const result=await createRole({role_key:roleKey,role_name:roleName,role_name_ar:roleNameAr||undefined,description:description||undefined,workspace:workspace||undefined,permissionIds:[...selectedIds]});setIsSaving(false);if(result.success){setRoleKey("");setRoleName("");setRoleNameAr("");setDescription("");setWorkspace("");setSelectedIds(new Set());onSuccess();}else setSaveError(roleError(result.error));};
  const canSave=roleKey.trim().length>=2&&roleName.trim().length>=2&&!!workspace&&!isSaving; const grouped=(catalog??[]).reduce<Record<string,PermissionRow[]>>((acc,p)=>{(acc[p.resource]??=[]).push(p);return acc;},{}); const labels=workspaceLabels(locale==="ar"); const direction=locale==="ar"?"rtl":"ltr";
  return <Dialog open={open} onOpenChange={v=>!v&&onClose()}><DialogContent className="max-w-2xl max-h-[90vh] overflow-y-auto" dir={direction}><DialogHeader><DialogTitle>{messages.roles.newRole}</DialogTitle></DialogHeader><div className="space-y-4"><div className="space-y-3"><div className="grid grid-cols-2 gap-3"><div className="space-y-1.5"><Label htmlFor="role-key">{messages.roles.roleKey}</Label><Input id="role-key" value={roleKey} onChange={e=>setRoleKey(e.target.value)} disabled={isSaving}/></div><div className="space-y-1.5"><Label htmlFor="role-name">{messages.roles.roleName}</Label><Input id="role-name" value={roleName} onChange={e=>setRoleName(e.target.value)} disabled={isSaving}/></div></div><div className="grid grid-cols-2 gap-3"><div className="space-y-1.5"><Label>{locale==="ar"?"مساحة العمل":"Workspace"}</Label><Select value={workspace} onValueChange={v=>setWorkspace(v as Workspace)}><SelectTrigger><SelectValue placeholder={locale==="ar"?"اختر مساحة العمل":"Choose workspace"}/></SelectTrigger><SelectContent>{(Object.keys(labels) as Workspace[]).map(w=><SelectItem key={w} value={w}>{labels[w]}</SelectItem>)}</SelectContent></Select></div><div className="space-y-1.5"><Label htmlFor="role-name-ar">{messages.roles.roleNameArabic}</Label><Input id="role-name-ar" value={roleNameAr} onChange={e=>setRoleNameAr(e.target.value)} disabled={isSaving}/></div></div><div className="space-y-1.5"><Label htmlFor="role-desc">{messages.roles.descriptionLabel}</Label><Input id="role-desc" value={description} onChange={e=>setDescription(e.target.value)} disabled={isSaving}/></div></div><Separator/><div><h4 className="text-sm font-medium mb-2">{messages.roles.permissions}</h4>{catalogLoading?<div className="flex items-center justify-center py-8"><Loader2 className="h-5 w-5 animate-spin text-primary"/></div>:<div className="space-y-3 max-h-[40vh] overflow-y-auto pr-1">{Object.entries(grouped).map(([resource,perms])=>{const info=terminology.permissions.resources[resource];const count=perms.filter(p=>selectedIds.has(p.id)).length;return <Card key={resource}><CardHeader className="pb-2"><div className="flex items-center justify-between"><CardTitle className="text-sm">{info?.label||resource}</CardTitle><Badge variant={count?"default":"outline"}>{count} / {perms.length}</Badge></div></CardHeader><CardContent className="pt-0"><div className="grid grid-cols-2 gap-2">{perms.map(p=><label key={p.id} className="flex items-center gap-2 rounded-md border p-2.5 cursor-pointer"><Checkbox checked={selectedIds.has(p.id)} onCheckedChange={()=>togglePermission(p.id)}/><span className="text-sm">{terminology.permissions.actions[p.action]||p.action}</span></label>)}</div></CardContent></Card>})}</div>}</div>{saveError&&<div className="rounded-md bg-red-50 border border-red-200 p-3 flex items-start gap-2"><AlertTriangle className="h-4 w-4 text-red-600"/><p className="text-sm text-red-800">{saveError}</p></div>}<div className="flex justify-end gap-2"><Button variant="outline" onClick={onClose} disabled={isSaving}>{messages.common.cancel}</Button><Button onClick={handleSave} disabled={!canSave} className="gap-2">{isSaving?<Loader2 className="h-4 w-4 animate-spin"/>:<Save className="h-4 w-4"/>}{messages.roles.createRole}</Button></div></div></DialogContent></Dialog>;
}
