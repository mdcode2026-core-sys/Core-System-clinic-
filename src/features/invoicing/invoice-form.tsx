"use client";

import { useState, useId } from "react";
import { useRouter } from "next/navigation";
import { Card, CardContent, CardHeader, CardTitle } from "@/shared/components/ui/card";
import { Button } from "@/shared/components/ui/button";
import { Input } from "@/shared/components/ui/input";
import { Label } from "@/shared/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/shared/components/ui/select";
import { Textarea } from "@/shared/components/ui/textarea";
import { Plus, Trash2, Calculator, Save } from "lucide-react";
import { createManualInvoice } from "@/domain/invoicing/invoicing.actions";
import { calculateLineItem, calculateInvoiceTotals, formatCurrency } from "@/domain/invoicing/invoicing.calculator";
import { usePermissions } from "@/core/permissions/usePermissions";
import { useTenantId } from "@/core/auth/useTenantId";
import { useSystemPreferences } from "@/domain/system-preferences";
import { useI18n } from "@/core/i18n/I18nProvider";
import type { InvoiceFormState, InvoiceFormItem, PaymentTerms, PatientOption, ProcedureOption, SessionOption } from "@/domain/invoicing/invoicing.types";

interface Props { initialPatients: PatientOption[]; initialProcedures: ProcedureOption[]; initialSessions: SessionOption[]; initialPatientsError: string | null; initialProceduresError: string | null; initialSessionsError: string | null; }

export function InvoiceForm({ initialPatients, initialProcedures, initialSessions, initialPatientsError, initialProceduresError, initialSessionsError }: Props) {
  const router = useRouter(); const { tenantId } = useTenantId(); const { hasPermission, isLoading: permLoading } = usePermissions(); const { locale, invoice: t } = useI18n(); const { data: preferences } = useSystemPreferences(tenantId); const currency = preferences?.currency ?? "USD"; const idPrefix = useId();
  const [patients] = useState<PatientOption[]>(initialPatients); const [procedures] = useState<ProcedureOption[]>(initialProcedures); const [sessions] = useState<SessionOption[]>(initialSessions); const [saving, setSaving] = useState(false); const [error, setError] = useState<string | null>(initialPatientsError || initialProceduresError || initialSessionsError);
  const [form, setForm] = useState<InvoiceFormState>({ patient_id: "", session_id: null, payment_terms: "cash", notes: "", items: [] });
  const canCreate = !permLoading && hasPermission("invoices:create"); const permissionError = !permLoading && !canCreate ? t.form.permissionDenied : null;
  const paymentTerms: Array<{ value: PaymentTerms; label: string }> = [{ value: "cash", label: t.cash }, { value: "credit", label: t.creditCard }, { value: "installment", label: t.installment }]; const money = (value: number) => formatCurrency(value, currency, locale);
  function addItem() { const item: InvoiceFormItem = { tempId: `${idPrefix}-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`, procedure_id: null, description: "", quantity: 1, unit_price_subunits: 0, discount_amount_subunits: 0, discount_percent: null, discount_reason: null, tax_rate_percent: 16, tax_included: false }; setForm((p) => ({ ...p, items: [...p.items, item] })); }
  function removeItem(tempId: string) { setForm((p) => ({ ...p, items: p.items.filter((i) => i.tempId !== tempId) })); }
  function updateItem(tempId: string, updates: Partial<InvoiceFormItem>) { setForm((p) => ({ ...p, items: p.items.map((i) => i.tempId === tempId ? { ...i, ...updates } : i) })); }
  function handleProcedureSelect(tempId: string, procedureId: string | null) { const procedure = procedures.find((p) => p.id === procedureId); updateItem(tempId, procedure ? { procedure_id: procedureId, description: procedure.procedure_name, unit_price_subunits: procedure.base_price_subunits, tax_rate_percent: procedure.tax_rate_percent ?? 16, tax_included: procedure.tax_included ?? false } : { procedure_id: null }); }
  function getCalculations() { return calculateInvoiceTotals({ items: form.items.map((item) => ({ quantity: item.quantity, unitPriceSubunits: item.unit_price_subunits, discountAmountSubunits: item.discount_amount_subunits, discountPercent: item.discount_percent, taxRatePercent: item.tax_rate_percent, taxIncluded: item.tax_included })) }); }
  async function handleSubmit() { if (!canCreate) return; if (!form.patient_id) { setError(t.form.patientRequired); return; } if (form.items.length === 0) { setError(t.form.itemRequired); return; } setSaving(true); setError(null); try { const result = await createManualInvoice({ patient_id: form.patient_id, session_id: form.session_id, payment_terms: form.payment_terms, notes: form.notes || null, items: form.items.map((item) => ({ procedure_id: item.procedure_id, description: item.description, quantity: item.quantity, unit_price_subunits: item.unit_price_subunits, discount_amount_subunits: item.discount_amount_subunits, discount_percent: item.discount_percent, discount_reason: item.discount_reason, tax_rate_percent: item.tax_rate_percent })) }); if (result.success) router.push(`/invoices/${result.data.invoice_id}`); else setError(result.error); } catch (e) { console.error("[InvoiceForm] create failed", e); setError(t.form.permissionDenied); } finally { setSaving(false); } }
  const totals = getCalculations();
  return <div className="space-y-6" dir={locale === "ar" ? "rtl" : "ltr"}><div className="flex items-center justify-between"><h1 className="text-2xl font-bold">{t.form.createTitle}</h1><Button variant="outline" onClick={() => router.push("/invoices")}>{t.form.back}</Button></div>{permissionError && <div className="rounded-md bg-destructive/10 p-3 text-sm text-destructive">{permissionError}</div>}{error && <div className="rounded-md bg-destructive/10 p-3 text-sm text-destructive">{error}</div>}
    <Card><CardHeader><CardTitle>{t.form.invoiceData}</CardTitle></CardHeader><CardContent className="space-y-4"><div className="grid grid-cols-2 gap-4"><div><Label>{t.patient}</Label><Select value={form.patient_id} onValueChange={(v) => setForm((p) => ({ ...p, patient_id: v }))} disabled={!canCreate}><SelectTrigger><SelectValue placeholder={t.form.choosePatient} /></SelectTrigger><SelectContent>{patients.map((p) => <SelectItem key={p.id} value={p.id}>{p.first_name} {p.last_name} — {p.phone_primary}</SelectItem>)}</SelectContent></Select></div><div><Label>{t.form.optionalSession}</Label><Select value={form.session_id ?? "none"} onValueChange={(v) => setForm((p) => ({ ...p, session_id: v === "none" ? null : v }))} disabled={!canCreate}><SelectTrigger><SelectValue placeholder={t.form.chooseSession} /></SelectTrigger><SelectContent><SelectItem value="none">{t.form.noSession}</SelectItem>{sessions.map((s) => <SelectItem key={s.id} value={s.id}>{s.patient?.first_name} {s.patient?.last_name} — {new Date(s.session_started_at).toLocaleDateString(locale === "ar" ? "ar" : "en-US", { numberingSystem: "latn" })}</SelectItem>)}</SelectContent></Select></div></div><div className="grid grid-cols-2 gap-4"><div><Label>{t.form.paymentTerms}</Label><Select value={form.payment_terms} onValueChange={(v) => setForm((p) => ({ ...p, payment_terms: v as PaymentTerms }))} disabled={!canCreate}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{paymentTerms.map((option) => <SelectItem key={option.value} value={option.value}>{option.label}</SelectItem>)}</SelectContent></Select></div><div><Label>{t.form.notes}</Label><Textarea value={form.notes} onChange={(e) => setForm((p) => ({ ...p, notes: e.target.value }))} placeholder={t.form.notesPlaceholder} disabled={!canCreate} /></div></div></CardContent></Card>
    <Card><CardHeader className="flex flex-row items-center justify-between"><CardTitle>{t.form.items}</CardTitle>{canCreate && <Button onClick={addItem} variant="outline" size="sm"><Plus className="me-2 h-4 w-4" />{t.form.addItem}</Button>}</CardHeader><CardContent className="space-y-4">{form.items.length === 0 && <div className="py-4 text-center text-muted-foreground">{t.form.noItems}</div>}{form.items.map((item, index) => { const calc = calculateLineItem({ quantity: item.quantity, unitPriceSubunits: item.unit_price_subunits, discountAmountSubunits: item.discount_amount_subunits, discountPercent: item.discount_percent, taxRatePercent: item.tax_rate_percent, taxIncluded: item.tax_included }); return <div key={item.tempId} className="space-y-3 rounded-lg border p-4"><div className="flex items-center justify-between"><span className="font-medium">{t.form.lineItem.replace("{index}", String(index + 1))}</span>{canCreate && <Button variant="ghost" size="sm" onClick={() => removeItem(item.tempId)} aria-label={t.form.lineItem.replace("{index}", String(index + 1))}><Trash2 className="h-4 w-4 text-red-500" /></Button>}</div><div className="grid grid-cols-2 gap-3"><div><Label>{t.form.procedure}</Label><Select value={item.procedure_id ?? "custom"} onValueChange={(v) => handleProcedureSelect(item.tempId, v === "custom" ? null : v)} disabled={!canCreate}><SelectTrigger><SelectValue placeholder={t.form.chooseProcedure} /></SelectTrigger><SelectContent><SelectItem value="custom">{t.form.custom}</SelectItem>{procedures.map((p) => <SelectItem key={p.id} value={p.id}>{p.procedure_name} — {money(p.base_price_subunits)}</SelectItem>)}</SelectContent></Select></div><div><Label>{t.form.description}</Label><Input value={item.description} onChange={(e) => updateItem(item.tempId, { description: e.target.value })} disabled={!canCreate} /></div></div><div className="grid grid-cols-4 gap-3"><div><Label>{t.quantity}</Label><Input type="number" min={1} value={item.quantity} onChange={(e) => updateItem(item.tempId, { quantity: parseInt(e.target.value) || 1 })} disabled={!canCreate} /></div><div><Label>{t.form.unitPrice}</Label><Input type="number" step="0.01" min={0} value={item.unit_price_subunits / 100} onChange={(e) => updateItem(item.tempId, { unit_price_subunits: Math.round(parseFloat(e.target.value || "0") * 100) })} disabled={!canCreate} /></div><div><Label>{t.form.discountPercent}</Label><Input type="number" min={0} max={100} value={item.discount_percent ?? ""} onChange={(e) => updateItem(item.tempId, { discount_percent: e.target.value ? parseFloat(e.target.value) : null })} disabled={!canCreate} /></div><div><Label>{t.form.discountAmount}</Label><Input type="number" step="0.01" min={0} value={item.discount_amount_subunits / 100} onChange={(e) => updateItem(item.tempId, { discount_amount_subunits: Math.round(parseFloat(e.target.value || "0") * 100) })} disabled={!canCreate} /></div></div><div className="flex flex-wrap items-center gap-2 text-sm text-muted-foreground"><Calculator className="h-4 w-4" /><span>{t.form.lineTotal}: {money(calc.lineTotal)}</span><span>|</span><span>{t.tax}: {money(calc.taxAmount)}</span>{calc.discountAmount > 0 && <><span>|</span><span>{t.discount}: {money(calc.discountAmount)}</span></>}</div></div>; })}</CardContent></Card>
    <Card><CardContent className="pt-6"><div className="space-y-2"><div className="flex justify-between text-sm"><span>{t.form.subtotalLabel}</span><span>{money(totals.subtotal)}</span></div><div className="flex justify-between text-sm text-red-600"><span>{t.discount}</span><span>-{money(totals.discount)}</span></div><div className="flex justify-between text-sm"><span>{t.tax}</span><span>{money(totals.tax)}</span></div><div className="flex justify-between border-t pt-2 text-xl font-bold"><span>{t.form.totalLabel}</span><span>{money(totals.total)}</span></div></div><div className="mt-6 flex justify-end"><Button onClick={() => void handleSubmit()} disabled={saving || !canCreate} size="lg"><Save className="me-2 h-4 w-4" />{saving ? t.issuing : t.createNew}</Button></div></CardContent></Card>
  </div>;
}
