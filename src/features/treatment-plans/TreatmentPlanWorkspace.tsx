"use client";

import { useEffect, useState } from "react";
import { useSearchParams, useRouter } from "next/navigation";
import { Button } from "@/shared/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/shared/components/ui/card";
import { Badge } from "@/shared/components/ui/badge";
import { Plus, RefreshCw, ArrowRight, CheckCircle2, Link2 } from "lucide-react";
import { addTreatmentPlanItem, createTreatmentPlan, getTreatmentPlans, linkVisitToTreatmentPlan, updateTreatmentPlan, updateTreatmentPlanItem } from "@/domain/treatment-plan/treatment-plan.actions";
import type { TreatmentPlanRecord } from "@/domain/treatment-plan/treatment-plan.types";
import { useI18n } from "@/core/i18n/I18nProvider";

export function TreatmentPlanWorkspace() {
  const searchParams = useSearchParams(); const router = useRouter(); const { locale, terminology: t } = useI18n(); const p = t.treatmentPlans; const patientId = searchParams.get("patientId") || undefined; const sourceVisitId = searchParams.get("visitId");
  const [plans, setPlans] = useState<TreatmentPlanRecord[]>([]); const [selected, setSelected] = useState<TreatmentPlanRecord | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); const [showCreate, setShowCreate] = useState(false); const [title, setTitle] = useState(""); const [diagnosis, setDiagnosis] = useState(""); const [goals, setGoals] = useState(""); const [itemTitle, setItemTitle] = useState(""); const [itemDescription, setItemDescription] = useState("");
  const refresh = async (selectId?: string) => { setLoading(true); setError(null); try { const next = await getTreatmentPlans(patientId); setPlans(next); setSelected(selectId ? next.find(x => x.id === selectId) ?? null : selected ? next.find(x => x.id === selected.id) ?? null : next[0] ?? null); } catch (e) { setError(e instanceof Error ? e.message : p.loadFailed); } finally { setLoading(false); } };
  useEffect(() => { let active = true; void getTreatmentPlans(patientId).then(next => { if (!active) return; setPlans(next); setSelected(next[0] ?? null); }).catch(e => { if (active) setError(e instanceof Error ? e.message : p.loadFailed); }).finally(() => { if (active) setLoading(false); }); return () => { active = false; }; }, [patientId, p.loadFailed]);
  const run = async (fn: () => Promise<unknown>, selectId?: string) => { setError(null); try { await fn(); await refresh(selectId); } catch (e) { setError(e instanceof Error ? e.message : p.actionFailed); } };
  const create = async () => { if (!patientId) { setError(p.mustSelectPatient); return; } await run(async () => { const id = await createTreatmentPlan({ patientId, sourceVisitId, title, diagnosisSummary: diagnosis, goals }); setTitle(""); setDiagnosis(""); setGoals(""); setShowCreate(false); await refresh(id); }); };
  const addItem = async () => { if (!selected || !itemTitle.trim()) return; await run(() => addTreatmentPlanItem({ treatmentPlanId: selected.id, title: itemTitle, description: itemDescription }), selected.id); setItemTitle(""); setItemDescription(""); };
  const planStatus = (status: string) => p.status[status as keyof typeof p.status] || status; const itemStatus = (status: string) => p.itemStatus[status as keyof typeof p.itemStatus] || status;
  return <div className="mx-auto max-w-6xl space-y-6" dir={locale === "ar" ? "rtl" : "ltr"}><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h1 className="text-3xl font-bold">{p.title}</h1><p className="mt-1 text-muted-foreground">{p.description}</p></div><div className="flex gap-2"><Button variant="outline" onClick={() => void refresh()} disabled={loading}><RefreshCw className="me-2 h-4 w-4" />{p.refresh}</Button>{patientId && <Button onClick={() => setShowCreate(true)}><Plus className="me-2 h-4 w-4" />{p.newPlan}</Button>}</div></div>{error && <div className="rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}{showCreate && <Card><CardHeader><CardTitle>{p.createTitle}</CardTitle></CardHeader><CardContent className="grid gap-3 md:grid-cols-2"><input className="rounded-md border p-3" placeholder={p.planTitle} value={title} onChange={e => setTitle(e.target.value)} /><input className="rounded-md border p-3" placeholder={p.diagnosis} value={diagnosis} onChange={e => setDiagnosis(e.target.value)} /><textarea className="min-h-24 rounded-md border p-3 md:col-span-2" placeholder={p.goals} value={goals} onChange={e => setGoals(e.target.value)} /><div className="flex gap-2 md:col-span-2"><Button onClick={() => void create()} disabled={!title.trim()}>{p.create}</Button><Button variant="outline" onClick={() => setShowCreate(false)}>{p.cancel}</Button></div></CardContent></Card>}{!patientId && plans.length === 0 && !loading && <Card><CardContent className="p-8 text-center text-muted-foreground">{p.noPlans}</CardContent></Card>}{loading && plans.length === 0 && <Card><CardContent className="p-8 text-center text-muted-foreground">{p.refresh}</CardContent></Card>}{plans.length > 0 && <div className="grid gap-6 lg:grid-cols-[280px_1fr]"><Card><CardHeader><CardTitle>{p.plans} ({plans.length})</CardTitle></CardHeader><CardContent className="space-y-2">{plans.map(plan => <button key={plan.id} onClick={() => setSelected(plan)} className={`w-full rounded-lg border p-3 text-start ${selected?.id === plan.id ? "border-primary bg-primary/5" : ""}`}><div className="font-medium">{plan.title}</div><div className="mt-1 text-xs text-muted-foreground">{plan.patient_name || p.patientFallback}</div><Badge className="mt-2">{planStatus(plan.status)}</Badge></button>)}</CardContent></Card>{selected ? <div className="space-y-4"><Card><CardHeader><div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><CardTitle>{selected.title}</CardTitle><div className="flex gap-2"><Badge>{planStatus(selected.status)}</Badge>{selected.status === "draft" && patientId && <Button size="sm" onClick={() => void run(() => updateTreatmentPlan(selected.id, { status: "active" }), selected.id)}>{p.activate}</Button>}{selected.status === "active" && patientId && <Button size="sm" variant="outline" onClick={() => void run(() => updateTreatmentPlan(selected.id, { status: "completed" }), selected.id)}><CheckCircle2 className="me-2 h-4 w-4" />{p.completePlan}</Button>}</div></div></CardHeader><CardContent className="space-y-3 text-sm"><div><span className="text-muted-foreground">{p.patient}: </span>{selected.patient_name || p.patientFallback}</div>{selected.diagnosis_summary && <div><span className="text-muted-foreground">{p.diagnosisLabel}: </span>{selected.diagnosis_summary}</div>}{selected.goals && <div><span className="text-muted-foreground">{p.goalsLabel}: </span>{selected.goals}</div>}<div className="text-muted-foreground">{p.linkedVisits}: {selected.visits.length}</div></CardContent></Card><Card><CardHeader><CardTitle>{p.activities} ({selected.items.length})</CardTitle></CardHeader><CardContent className="space-y-3">{patientId && <div className="grid gap-2 md:grid-cols-[1fr_1fr_auto]"><input className="rounded-md border p-3" placeholder={p.activityName} value={itemTitle} onChange={e => setItemTitle(e.target.value)} /><input className="rounded-md border p-3" placeholder={p.shortDescription} value={itemDescription} onChange={e => setItemDescription(e.target.value)} /><Button onClick={() => void addItem()} disabled={!itemTitle.trim()}><Plus className="me-2 h-4 w-4" />{p.add}</Button></div>}{selected.items.length === 0 ? <p className="py-4 text-center text-sm text-muted-foreground">{p.noActivities}</p> : selected.items.map(item => { const linked = selected.visits.some(v => v.treatment_plan_item_id === item.id); return <div key={item.id} className="rounded-lg border p-3"><div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><div><span className="font-medium">{item.sequence_no}. {item.title}</span>{item.description && <p className="text-sm text-muted-foreground">{item.description}</p>}</div><div className="flex items-center gap-2"><Badge variant="outline">{itemStatus(item.status)}</Badge>{patientId && <select aria-label={p.activities} className="rounded-md border p-2 text-sm" value={item.status} onChange={e => void run(() => updateTreatmentPlanItem(item.id, { status: e.target.value as any }), selected.id)}>{Object.entries(p.itemStatus).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select>}{sourceVisitId && !linked && <Button size="sm" variant="outline" onClick={() => void run(() => linkVisitToTreatmentPlan(selected.id, sourceVisitId, item.id), selected.id)}><Link2 className="me-2 h-4 w-4" />{p.linkVisit}</Button>}</div></div></div>; })}</CardContent></Card>{sourceVisitId && !selected.visits.some(v => v.visit_id === sourceVisitId) && <Button variant="outline" onClick={() => void run(() => linkVisitToTreatmentPlan(selected.id, sourceVisitId), selected.id)}>{p.linkCurrentVisit}</Button>}<Button variant="ghost" onClick={() => router.push("/clinical")}><ArrowRight className="me-2 h-4 w-4" />{p.backToMedical}</Button></div> : <Card><CardContent className="p-8 text-center text-muted-foreground">{p.selectPlan}</CardContent></Card>}</div>}</div>;
}
