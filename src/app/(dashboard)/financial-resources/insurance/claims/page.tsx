import { cookies } from "next/headers";
import { redirect } from "next/navigation";
import { createClient } from "@/infrastructure/supabase/server";
import { resolveTenantId } from "@/core/auth/resolveTenantId";
import { getEffectivePermissions } from "@/core/permissions/permissionEngine";
import { InsuranceClaimsOperations } from "@/features/financial-resources/insurance-claims-operations";
export default async function FinancialResourcesInsuranceClaimsPage(){const db=await createClient();const{data:{user},error}=await db.auth.getUser();if(error||!user)redirect("/login");const tenantId=await resolveTenantId(user.id);if(!tenantId)redirect("/login");const permissions=await getEffectivePermissions(user.id,tenantId);if(!permissions.includes("insurance:read"))redirect("/");const locale=(await cookies()).get("core-system-locale")?.value==="en"?"en":"ar";const[tenant,patients,profiles,invoices,claims]=await Promise.all([db.from("master_tenants").select("currency").eq("id",tenantId).maybeSingle(),db.from("clinic_patients").select("id,first_name,last_name").eq("tenant_id",tenantId).is("deleted_at",null).order("first_name").limit(1000),db.from("patient_insurance_profiles").select("id,patient_id,payer_name,policy_number,member_number").eq("tenant_id",tenantId).is("deleted_at",null).order("created_at",{ascending:false}).limit(1000),db.from("clinic_invoices").select("id,invoice_number,patient_id,amount_due_subunits").eq("tenant_id",tenantId).order("invoice_date",{ascending:false}).limit(1000),db.from("insurance_claims").select("id,patient_id,insurance_profile_id,invoice_id,claim_reference,amount_claimed_subunits,amount_reconciled_subunits,status").eq("tenant_id",tenantId).is("deleted_at",null).order("created_at",{ascending:false}).limit(1000)]);return <div className="p-4 sm:p-6 lg:p-8"><InsuranceClaimsOperations locale={locale} currency={tenant.data?.currency??"JOD"} patients={patients.data??[]} profiles={profiles.data??[]} invoices={invoices.data??[]} claims={claims.data??[]}/></div>}
